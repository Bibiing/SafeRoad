import 'package:flutter/material.dart';

// TODO(admin): implementasi oleh Nabil — Admin Features.

/// Layar kelola laporan oleh admin (update status, dll).
class ManageReportsScreen extends StatelessWidget {
  const ManageReportsScreen({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('Manage Reports — Coming Soon')),
      );
}
