import 'package:flutter/material.dart';

// TODO(admin): implementasi oleh Nabil — Admin Features.

/// Layar validasi laporan oleh admin.
class ValidateReportScreen extends StatelessWidget {
  const ValidateReportScreen({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('Validate Report — Coming Soon')),
      );
}
